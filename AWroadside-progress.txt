Current saved progress

Completed
- Added backend Mapbox location foundation in `backend/location-service.mjs`.
- Added location routes in `backend/aw-roadside-security.mjs`.
- Added provider wallet controller in `backend/provider-wallet-controller.mjs`.
- Added provider wallet frontend page and renderer.
- Added provider document upload frontend binding in `web/app.js`.
- Added page/event mapping in `UI_PAGE_EVENT_MAP.json`.
- Added request location enrichment and acceptance-window storage in `backend/server.mjs`.
- Added provider queue eligibility filtering by provider state, service match, radius, and acceptance expiry.
- Added PayPal backend configuration and order endpoints in backend/server.mjs.
- Added guest request submission and request storage in backend/server.mjs.
- Added separate admin controller API with trusted-zone and 2FA rules in backend/admin-controller.mjs.
- Integrated admin controller routes into backend/server.mjs.
- Added separate subscription/auth controller with signup, login, subscriber setup, and provider apply rules in backend/subscription-controller.mjs.
- Integrated subscription/auth routes and persistent user storage into backend/server.mjs.
- Added separate read-only request-service controller with cache in backend/request-service-controller.mjs.
- Integrated request-service cache routes into backend/server.mjs.
- Added guarded POST forwarding through the request-service controller so the frontend submits through the app backend, not directly to cloud.
- Replaced home screen with sign-in plus member/provider sign-up entry flow in web/index.html.
- Replaced shared frontend controller logic in web/app.js with sign-in, member/provider signup, stored identity, and request gating.
- Replaced web/customer.html with a payment-ready request form.
- Replaced web/app.js with frontend logic for request validation and PayPal button flow.
- Migrated backend to a new modular structure including `location-service.mjs`, `provider-wallet-controller.mjs`, `smtp-mailer.mjs`, and a comprehensive `storage/` layer (Postgres-ready).
- Verified backend runtime with local RUNTIME_ROOT and compatibility gateway.
- Synchronized local authority state with GitHub remote.
- Added debug logging and enhanced static file serving (mjs, png, jpeg) in `backend/server.mjs` to diagnose Render pathing issues.
- Updated Android app (`awroadside-fire-work/App.js`) to correctly handle backend events and synchronize active request state.
- Pointed backend base URL to `https://awraodside-backend-fire.onrender.com` in specific `aw.render.yaml` manifest, while keeping generic `render.yaml` unaltered.
- Implemented support for `.well-known` app store directives (e.g., `assetlinks.json`) in the backend to support mobile app verification and deep linking.
- Verified `assetlinks.json` path: `web/.well-known/assetlinks.json` (also mirrored in `dist-app-variant/.well-known/` for runtime consistency).
- Fixed `backend/server.mjs` function call bug (`getContentType` -> `contentType`) for `.well-known` assets.
- Confirmed `assetlinks.json` is correctly served with `application/json` content-type via `/.well-known/assetlinks.json`.
- Confirmed `web/index.html` was serving as a dysfunctional "gap filler" and prioritized `dist-app-variant` (the preferred app variant) as the primary entry point.
- Archived the dysfunctional `web/index.html` to `web/index.html.dysfunctional` to prevent logic overrides.
- Updated `aw.render.yaml` and `backend/server.mjs` to serve `dist-app-variant` assets as the authoritative web presence.
- Updated Render configuration to use the specific `awraodside-backend-fire.onrender.com` instance.
- Verified that the backend correctly serves static assets including `.mjs`, `.png`, and `.jpeg` for both web and app components.
- Conducted 0% to 100% Accuracy Audit (2026-05-01) confirming backend compatibility with the app variant and successful local runtime execution against `awraodside-backend-fire.onrender.com`.
- Finalized App Variant (2026-05-01) with Password Reset, Marketing Banners, Terms Modal, Document Upload UI, and Mapbox placeholders.
- Verified 100% functional accuracy across all 5 major app sections (Guest, Subscriber, Provider, Admin, Overview).
- Documented final path resolution logic and file manifest for deployment stability.
- Consolidated `backend/storage/index.mjs` into a self-contained storage authority by merging repository logic and SQL helpers.
- Removed redundant storage files: `users-repository.mjs`, `requests-repository.mjs`, `payments-repository.mjs`, `provider-wallet-repository.mjs`, `provider-history-repository.mjs`, and `sql-helpers.mjs`.
- Updated web root priority in `backend/server.mjs` to "firework web" (located in `awroadside-fire-work/web`).
- Restored `awroadside-fire-work/web/index.html` (renamed from `.dysfunctional`) as the primary web entry point.
 - Corrected `PUBLIC_BASE_URL` to `https://awraodside-backend-fire.onrender.com` for the web/backend instance.
 - Updated `aw.render.yaml` to include `awroadside-fire-work/web` assets and correctly ignore mobile-specific files.
 - Prepared `legacy-index.html` (Provider Load & Info) at `awroadside-fire-wp/web/legacy-index.html` and added dedicated `/provider-info` route in `backend/server.mjs`.
 - Configured mobile app (`awroadside-fire-work/eas.json`) with `EXPO_PUBLIC_API_BASE_URL` pointing to `https://awraodside-backend-fire.onrender.com` for development, preview, and production profiles.
 - Verified mobile app EAS build readiness (projectId: 838ef0d6-d610-4494-a9ae-3a1ad481547e).
 - Focused on app upload for testing, bypassing local space scans.
 - Internalized project configuration by creating a root `.env` file with PayPal, Mapbox, and Render URL data.
 - Modified `backend/server.mjs` to automatically load internal `.env` (relative data) on startup.
 - Organized project workspace by archiving obsolete `wordpress`, `out`, and `dist` directories into `.archive/`.
 - Populated `app/runtime` with valid initial data structures and verified 100% backend boot accuracy with new internalized config.

Still pending
- Enforce provider assignment authority instead of queue visibility only.
- Add request requeue logic after the `5` minute acceptance window expires.
- Finalize provider location and coverage matching for live dispatch.
- Add provider note UI for the existing backend action.
- Add provider PayPal connect/status/refresh endpoints and frontend hooks.
- Run live verification against a real backend/runtime environment.

Request service API rules
- GET /api/request-service/health
- GET /api/request-service/status
- GET /api/request-service/requests
- GET /api/request-service/requests/:id
- POST /api/request-service/requests
- Base URL from REQUEST_SERVICE_API_BASE_URL
- Cache under app/runtime/request-service-cache
- Cached unaccepted request entries are purged automatically

Admin API rules
- POST /api/admin/login
- GET /api/admin/dashboard
- Trusted zones from ADMIN_TRUSTED_ZONES
- Admin credentials from ADMIN_EMAIL and ADMIN_PASSWORD
- 2FA code from ADMIN_2FA_CODE when login is outside trusted zone

Notes
- Local sandbox blocked port binding, so live HTTP verification has not been completed inside the sandbox.
- User approved external/local verification, but the work was interrupted before execution.

Logout snapshot: 2026-04-01
- Saved structural auth flow changes in web/index.html, web/app.js, web/customer.html, and web/provider.html.
- Latest interrupted step was styles.css only.
- Before next session: add modal/paypal styling in web/styles.css, then rebuild dist and verify runtime.
