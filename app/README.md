# Runtime App Folder

This folder is populated by the local Java server at runtime.

Expected generated paths:

- `app/runtime/manifest.json`
- `app/runtime/reports/startup-report.txt`
- `app/runtime/logs/session.log`
