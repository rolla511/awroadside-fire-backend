AW Roadside next-session handoff
Date: 2026-04-25

Current authority paths
- Backend/web runtime: /Users/user/IdeaProjects/untitled
- Active Expo mobile app: /Users/user/IdeaProjects/untitled/awroadside-fire-work

What is now in place
- Protected backend routes for request, provider action, payment quote, payment create/capture
- Compatibility gateway with status, resolve, repository, manifest, and acknowledge endpoints
- Runtime repository with request counts, stale timestamps, and acknowledged variants
- Website runtime now displays variant handshake state
- Provider wallet route and provider wallet controller are in place
- Provider document upload is wired in `web/app.js`
- Mapbox backend location foundation is in place
- Request creation now geocodes location when Mapbox is configured
- Provider queue now filters visible requests by eligibility instead of exposing the entire queue

New endpoints to use next
- GET /api/compat/status
- GET /api/compat/resolve
- GET /api/compat/repository
- GET /api/compat/manifest
- POST /api/compat/acknowledge
- GET /api/aw-roadside/location/config
- GET /api/aw-roadside/location/geocode?q=...
- GET /api/aw-roadside/location/isochrone?longitude=...&latitude=...&minutes=...&profile=...

Main unresolved product work
- Mobile UI in awroadside-fire still needs:
  - service quote / agree flow
  - provider fee display
  - provider work and payout history
  - visible terms text for guest, subscriber, provider
- Backend still needs:
  - assignment authority module for provider selection
  - `20` mile radius enforcement at assignment time
  - `5` minute acceptance timeout and requeue logic
  - provider note UI hookup
  - provider PayPal connect/status/refresh endpoints
  - dispatch scoring / coverage / shift rules finalized for live service

Android verification status
- Remote EAS Android AAB passed previously
- Local Gradle bundle did not complete and did not produce a local artifact
- Next local verification should target APK first, then AAB if needed

iOS status
- iOS/TestFlight path was not completed in this session
- awroadside-fire remains the only mobile authority path for iOS work

Do not do next
- Do not create a new Expo project name just to continue the same product
- Do not activate Desktop Expo 53 or other legacy variants as live authority
- Do not move pricing or financial rules into UI-only logic
- Do not bypass provider geographic filtering for live dispatch
- Do not treat plain location strings as sufficient for real provider assignment

Tomorrow start point
- Start in `backend/server.mjs`
- Read `backend/location-service.mjs`
- Read `backend/aw-roadside-security.mjs`
- Read `UI_PAGE_EVENT_MAP.json`
- Read `UI_EVENT_TEST_CHECKLIST.md`
- Continue from provider-assignment enforcement

Things the backend still needs
- Provider assignment authority
- Radius enforcement at assignment
- Timeout/requeue
- Live provider selection by coordinates and service type
- Real authenticated end-to-end verification

Things the product still needs
- Guest/subscriber/provider/admin checklist completion
- Provider wallet PayPal competency
- Reliable live events at 100% capacity
- No long ETA or impossible dispatch geography
